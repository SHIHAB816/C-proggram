#include <stdio.h>

int main() {
    
    printf("the size of int %lu bytes\n",sizeof(int));
    printf("the size of float %lu bytes\n",sizeof(float));
    printf("the size of double %lu bytes\n",sizeof(double) );
    printf("the size of charecter %lu bytes\n ",sizeof(char) );
    
    return 0;
}
