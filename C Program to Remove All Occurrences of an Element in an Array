#include <stdio.h>

int main(){
    int n, key;
    scanf("%d", &n);
    int a[n], j=0;
    for(int i=0;i<n;i++) scanf("%d",&a[i]);
    scanf("%d",&key);

    for(int i=0;i<n;i++)
        if(a[i]!=key) a[j++] = a[i];

    for(int i=0;i<j;i++) printf("%d ",a[i]);
    return 0;
}
