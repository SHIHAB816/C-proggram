#include <stdio.h>

int main() {
    int n, i, j;
    printf("Enter rows: ");
    if (scanf("%d", &n) != 1) return 0;
    for (i = n; i >= 1; i--) {
        for (j = 0; j < n - i; j++) printf(" ");
        for (j = 0; j < i; j++) printf("*");
        printf("\n");
    }
    return 0;
}
